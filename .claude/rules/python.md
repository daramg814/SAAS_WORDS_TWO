---
paths:
  - "**/*.py"
---
# Python 규칙
- Python 3.11 이상을 기준으로 타입 힌트를 사용한다.
- 네트워크·파일·DB 경계는 명시적 예외와 재시도/롤백을 갖는다.
- 결정론적 계산은 순수 함수로 분리하고 단위 테스트를 작성한다.
- 시간은 Asia/Seoul 인식 datetime으로 저장한다.
- 원자적 파일 교체는 같은 파일시스템의 임시 파일과 `os.replace`를 사용한다.

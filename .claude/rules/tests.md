---
paths:
  - "tests/**/*.py"
  - "qa/**/*.py"
---
# 테스트 규칙
- QA 전용 대체 구현을 만들지 않는다.
- 테스트는 운영 함수와 진입점을 직접 호출한다.
- ledger/캐시 병합의 정확성(재생성/재조회 방지, 정규화 키 병합, backlog 유실 방지)을 검증한다.
- 19/21 같은 수량 오류(`contracts.validate_title_set`)를 실패로 검증한다.
